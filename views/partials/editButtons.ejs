<div
    class="box-edit col-12 d-flex justify-content-center mt-5 mb-2 border rounded p-2"
>
    <a href="/products/<%= product.id%>/edit">
        <button type="submit" class="btn btn-outline-secondary mr-2">
            <i class="far fa-edit"></i> Editar Publicación
        </button>
    </a>
    
        <form action="<%= product.id %>/delete?_method=delete" method="POST">
            <button type="submit" class="btn btn-outline-danger mr-2">
                <i class="far fa-edit"></i> Eliminar Publicación
            </button>
        </form>

        <!-- ARREGLAR LA VENTANA MODAL QUE HAGA CONFIRMACION ANTES DE ELIMINAR -->
        <!-- Buttom Alert -->
        <!-- <div
        class="modal fade"
        id="Eliminar_Modal"
        tabindex="-1"
        role="dialog"
        aria-labelledby="Eliminar_Modal_label"
        aria-hidden="true"
    >
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="Eliminar_Modal_label">
                        ¿Está seguro que desea eliminar el producto?
                    </h5>
                    <button
                        type="button"
                        class="close"
                        data-dismiss="modal"
                        aria-label="Close"
                    >
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>

                <div class="modal-footer">
                    <button
                        type="button"
                        class="btn btn-outline-secondary text-secondary"
                        data-dismiss="modal"
                    >
                        NO
                    </button>
                    <form
                        action="<%= product.id %>/delete?_method=delete"
                        method="POST"
                    >
                        <button
                            type="submit"
                            class="btn btn-outline-warning ml-2 text-warning"
                        >
                            SI
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div> -->
    </div>
</div>
